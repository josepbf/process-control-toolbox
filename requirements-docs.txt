# Documentation site only. Kept separate from requirements.txt so the library
# itself stays dependency-light (see the note at the top of requirements.txt).
#
#   python3 -m venv .venv-docs
#   .venv-docs/bin/pip install -r requirements-docs.txt
#   .venv-docs/bin/mkdocs serve

mkdocs-material==9.7.7
mkdocstrings[python]==1.0.6
black==26.5.1          # mkdocstrings uses it to format rendered signatures
